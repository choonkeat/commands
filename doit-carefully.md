$ARGUMENTS

Write down, into ./tasks/{yyyy-mm-dd-HHMMSS in local time}-{task}.md file, a detailed plan to implement in small steps. 

In each step
  1. have test to verify that the new code works and that we did not introduce regression bugs
  2. then update the detailed plan .md file to indicate progress
  3. git commit only the relevant files (do not bluntly git add everything)

